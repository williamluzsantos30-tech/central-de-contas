# Modo DEMO (dados fictícios do mockDb, sem Supabase, já logado como admin).
# Sem segredos: as variáveis ficam VAZIAS de propósito — é isso que liga o
# modo DEMO. Uso: vite --mode demo (config "dev-demo" em .claude/launch.json).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
