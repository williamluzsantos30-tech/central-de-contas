/**
 * Comercial › Metas — reaproveita o MESMO painel do Marketing em modo "metas":
 * mesmos blocos/cards, mas cada card permite definir/editar a meta inline
 * (ver MarketingFunnelPanel).
 */
import MarketingFunnelPanel from './MarketingFunnelPanel'

export default function MetasPanel() {
  return <MarketingFunnelPanel modo="metas" />
}
