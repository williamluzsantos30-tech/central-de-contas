# Dependências
node_modules

# Build outputs
dist
dist.zip
*.zip

# Variáveis de ambiente — contêm secrets, NUNCA versionar
.env
.env.local
.env.*.local

# Caches
.vite
*.tsbuildinfo

# Estado local do Supabase CLI — contem project-ref, host do banco,
# email do dono da conta. Nada disso e senha, mas mapea a superficie
# e nao pertence ao repo. Cada dev que rodar `supabase link` gera o
# proprio localmente.
supabase/.temp/

# Configs locais de IDE / agente
.claude
.vscode
.idea

# Sistema operacional
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
